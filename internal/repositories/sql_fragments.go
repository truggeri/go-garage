package repositories

// sqlAndVehicleID is the SQL fragment used to filter by vehicle_id.
const sqlAndVehicleID = " AND vehicle_id = ?"

// sqlLimit is the SQL LIMIT clause fragment.
const sqlLimit = " LIMIT ?"

// sqlOffset is the SQL OFFSET clause fragment.
const sqlOffset = " OFFSET ?"
